[mypy]
# Only check core packages to avoid noise from test files and utility scripts.
files = agents,worker,mcp
strict = True
ignore_missing_imports = True
show_error_codes = True
pretty = True

# Disable checks for generated/third-party code if present
[mypy-*/tests.*]
ignore_errors = True
